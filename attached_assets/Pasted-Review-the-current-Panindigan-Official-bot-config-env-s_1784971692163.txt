Review the current Panindigan Official bot config/env setup (discord.js v14 + Lavalink v4) and confirm what's needed:

Discord Bot Token — confirm this is the only Discord-side credential required for the bot to log in and run commands (slash commands, moderation, economy, etc.)
Lavalink connection — list what's needed:
Lavalink node host/IP
Port
Password (application.yml)
Secure (SSL) true/false
Which Lavalink client library we're using (e.g. lavalink-client, shoukaku, erela.js) and confirm it's compatible with Lavalink v4
Whether we're self-hosting Lavalink or using a public/free node, and if self-hosting, where (same host as bot, or separate service)
Professional logging setup — implement end-to-end, not just proposed: Core logger
Structured logger (Pino preferred for performance, or Winston) as a shared singleton/module imported across the whole codebase — remove all raw console.log/console.error calls
Log levels: debug, info, warn, error, fatal — configurable via LOG_LEVEL env var (e.g. debug in dev, info in prod)
Consistent ISO 8601 timestamps and consistent field naming (level, time, module, msg, meta) across all log entries so they're easy to grep/parse later
Transports
Console transport: pretty-printed, colorized, human-readable (dev only)
File transport: structured JSON, rotated daily and by max size (e.g. pino-roll or winston-daily-rotate-file), with retention policy (e.g. keep 14 days) so disk doesn't fill up
Separate log files per severity where useful (e.g. error.log combined view of only warn/error/fatal, plus full combined.log)
Module-level tracing
Child loggers per module/subsystem (commands, events, music, database, economy, moderation, tickets, giveaways, ai) so every log line is traceable to its source without manual tagging
Command/interaction logging: log every command execution with user ID, guild ID, command name, args (redacted if sensitive), execution time, and success/failure
Error capture
Global handlers for unhandledRejection and uncaughtException that log via the logger (with full stack trace) instead of crashing silently or dumping to stderr
Dedicated error capture for: Discord API errors (rate limits, permission errors, disconnects, gateway resumes), Lavalink node events (connect/disconnect/reconnect, track exceptions, node errors, player errors), and MongoDB connection errors (disconnects, slow queries)
Wrap all command handlers and event listeners in a try/catch that logs the error with context (command name, guild, user) instead of letting it bubble unhandled
Monitoring & alerting
Webhook/Discord channel integration that forwards error/fatal logs to a private staff channel in real time, rate-limited/batched to avoid spam
Startup/shutdown logs: bot ready, guild count, shard status, Lavalink node connection status, DB connection status
Periodic health-check log (e.g. every N minutes: memory usage, uptime, active voice connections, queued jobs) for spotting slow leaks/degradation
Basic metrics worth tracking in logs or a lightweight metrics store: commands run per hour, errors per hour, active music sessions, guild join/leave events
Security & hygiene
Confirm logs never leak secrets (tokens, passwords, DB URIs, API keys) — add redaction for known sensitive fields at the logger config level, not per call site
Sanitize user-provided input before logging (avoid log injection via newlines/control chars)
Deployment considerations
Confirm log file location works correctly on the current production host (24/7 hosting setup) — persistent volume if using ephemeral containers, since logs shouldn't disappear on redeploy
Document how to tail/query logs in production (e.g. pm2 logs, docker logs, or wherever it's hosted)

Output: implement the logger module, wire it into all subsystems, add the Discord webhook alert channel, and give me a before/after summary of what changed plus a short doc on how to read/query the logs going forward.